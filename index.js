import config from "./config.js";

// foi criando o arquivo: npm init -y

// criado as pasta index.js
//config.js

// o arquivo package.json foi editado
// "type": "module",
// depois foi colocado

/*"start": "node index.js",

foi criando o arqui .env 
e foi digtado as variaveis de ambiente

TOKEN=123
ID_GUILD=1000
GOOGLE_URL=https://www.google.com
ROLES=admin,moderator,teacher
JSON_CONFIG={"name": "emanoel", "id":"1"}
NODE_ENV=prod

em seguida o arquivo package.json foi editado novamente
    "start": "node --env-file .env index.js",
depois foi criado 

os dados da pasta config.js chamando as variaveis de ambiente

*/





//"start": "node --env-file .env index.js",
//console.log(process.env.ROLES);
console.log(config); 


